checks for merchant:
  - schema:
      fail:
        when required column missing: [merchant_id, merchant_name]
        when wrong column type:
          merchant_id: integer
          merchant_name: string
